const email1 = 'admin_' + Date.now() + '@test.com'; const email2 = 'pharm_' + Date.now() + '@test.com'; const pwd = 'password123'; async function test() { const r1 = await fetch('http://localhost:3000/api/auth/register', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: email1, password: pwd, confirmPassword: pwd, role: 'admin' }) }); console.log(r1.status, await r1.text()); const r2 = await fetch('http://localhost:3000/api/auth/register', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: email2, password: pwd, confirmPassword: pwd, role: 'pharmacist' }) }); console.log(r2.status, await r2.text()); } test();
