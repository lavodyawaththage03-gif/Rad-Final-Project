const email = 'test_student_' + Date.now() + '@test.com'; const pwd = 'password123'; async function test() { const r1 = await fetch('http://localhost:3000/api/auth/register', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password: pwd, confirmPassword: pwd, role: 'student' }) }); const c = r1.headers.get('set-cookie'); const r2 = await fetch('http://localhost:3000/api/profile/student', { method: 'PUT', headers: { 'Content-Type': 'application/json', 'Cookie': c }, body: JSON.stringify({ address: '123 Test St' }) }); const t = await r2.text(); console.log(r2.status, t); } test();
