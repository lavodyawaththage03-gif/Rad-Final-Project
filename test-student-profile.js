const mongoose = require('mongoose'); require('dotenv').config({path: '.env.local'}); const uri = process.env.MONGODB_URI; mongoose.connect(uri).then(() => { const StudentProfile = require('./lib/models/StudentProfile').StudentProfile; console.log('Successfully loaded StudentProfile!'); process.exit(0); }).catch(err => { console.error('Connection error:', err); process.exit(1); });
